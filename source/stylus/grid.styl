.row {
	position: fixed
	width: 100%
	height: 100%
  max-width: maximum-width
	pointer-events: none
  opacity: 0
  z-index: 10000

	for i in (0..20) {
		.column-{i} {
  		width: 1px
  		margin-left: lookup('percentage-' + i)
  		height: 100%
  		position: absolute
  		float: left
  		background-color: grid-color
  	}
	}
}
