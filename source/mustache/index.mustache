<!DOCTYPE html>
<html>
  <head>
    {{> head }}
    <title>Gilabert</title>
  </head>
  <body id='myself'>
    <h1 id='page-title'>Ramon Gilabert</h1>
    <nav>
      <figure></figure>
      <div id='indicator'></div>
      {{# myself }}
      <div><a id='navigation-{{ kind }}' href=''>{{ kind }}</a></div>
      {{/ myself }}
    </nav>
    {{# myself }}
    <section id="{{ kind }}">
      <figure></figure>
      <div class='wrapper'>
        <h2>{{ title }}</h2>
        <p><strong>{{ span }}</strong>{{ subtitle }}</p>
        <button id="{{ kind }}-button">read more</button>
      </div>
    </section>
    {{/ myself }}
  </body>
</html>
